[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "eagle"
version = "0.1.0"
description = "Eagle AI Finance Controller"

[tool.setuptools.packages.find]
where = ["src"]
